{
  "name": "investment-portfolio-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-contained investment portfolio tracker with live prices, alerts, corporate events, imports, and watchlists.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "worker": "node --env-file=.env src/worker.js",
    "test": "node --test test/*.test.js"
  }
}
